At first open the main folder of the project on your terminal.
Install the necessary packages with the following commands

npm install
npm install -g grunt-cli
npm install -g browsersync

now you can start the project with the command 

grunt